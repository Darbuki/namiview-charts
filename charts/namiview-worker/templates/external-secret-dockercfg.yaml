apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ include "namiview-worker.dockercfgSecretName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "namiview-worker.labels" . | nindent 4 }}
spec:
  refreshInterval: {{ .Values.vault.refreshInterval | quote }}
  secretStoreRef:
    name: {{ .Values.vault.clusterSecretStore }}
    kind: ClusterSecretStore
  target:
    name: {{ include "namiview-worker.dockercfgSecretName" . }}
    template:
      type: kubernetes.io/dockerconfigjson
  data:
    - secretKey: .dockerconfigjson
      remoteRef:
        key: {{ .Values.vault.paths.dockerReg }}
        property: .dockerconfigjson
