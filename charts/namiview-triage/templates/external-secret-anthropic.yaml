{{- if .Values.externalSecrets.enabled }}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ include "namiview-triage.anthropicSecretName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "namiview-triage.labels" . | nindent 4 }}
spec:
  refreshInterval: {{ .Values.externalSecrets.refreshInterval | quote }}
  secretStoreRef:
    name: {{ required "externalSecrets.clusterSecretStore must be set" .Values.externalSecrets.clusterSecretStore }}
    kind: ClusterSecretStore
  target:
    name: {{ include "namiview-triage.anthropicSecretName" . }}
  data:
    - secretKey: ANTHROPIC_API_KEY
      remoteRef:
        key: {{ required "externalSecrets.paths.anthropic must be set" .Values.externalSecrets.paths.anthropic }}
        property: ANTHROPIC_API_KEY
{{- end }}
